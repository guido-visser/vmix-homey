vMix integration for Homey

Trigger flows with vMix. A good example could be: When Streaming Started -> Turn on On Air light

It used the Web API for the condition (And) cards and it uses the TCP API for the trigger (When) cards.